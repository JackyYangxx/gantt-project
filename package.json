{
  "name": "gantt-project",
  "version": "1.2.2",
  "private": true,
  "workspaces": ["server", "client"],
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"npm -w server run dev\" \"npm -w client run dev\"",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "dev:electron": "concurrently -k \"npm -w server run dev\" \"npm -w client run dev\" \"wait-on http://localhost:3001/api/health && cross-env ELECTRON_DEV=true electron electron/main.js\"",
    "build:server": "node server/build.mjs",
    "build:client": "npm -w client run build",
    "build:electron": "npm run build:server && npm run build:client && electron-builder",
    "test:e2e": "cross-env NODE_PATH=node_modules npx playwright test",
    "reset-db": "npm -w server run reset-db"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "@playwright/test": "^1.62.0",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "electron": "^33.2.0",
    "electron-builder": "^26.0.0",
    "playwright": "^1.62.0",
    "wait-on": "^8.0.0"
  }
}
